/**
 * Hand-authored Tailwind extensions.
 *
 * Merged over the generated token values in `config/tailwind.tokens.js` by
 * `tailwind.config.js`. Edit freely — the generator never touches this file.
 * Keys set here override the corresponding generated token keys.
 */
export default {
	extend: {},
};
